---
import ContentPage from '../../components/ContentPage.astro';
import { getPage } from '../../data/pages';

const page = getPage('/news-events/press-releases');
if (!page) return Astro.redirect('/404');
---

<ContentPage page={page} />
